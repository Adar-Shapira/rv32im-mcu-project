# Deliverable A — Source and Reuse Inventory

Step 1 of the RV32IM MCU final project. Answers one question for every component the project needs:
**does supplied code already exist, and if so may we use it?**

Nothing in this document changes any file. Every path was opened and verified.

---

## 1. Provenance of the reference tree

`Auxiliary/` holds four distinct kinds of material that must not be conflated.

| Provenance | Where | How it is identified |
| --- | --- | --- |
| **Hanan Ribo (lecturer)** | `Auxiliary/Lab 5/Auxilary/{DUT,TB,SIM,QUARTUS}/`, the PDFs at that level, `Auxilary/test1`–`test4/`, `Auxiliary/Benchmark Apps/`, `Auxiliary/Final Project 2026 definition.pdf` | Ten files carry `-- Copyright 20xx Hananya Ribo` on line 2. That header is the only authorship marker in the tree. |
| **The students, earlier labs** | `Auxiliary/Lab 5/Auxilary/Lab3/`, `Auxilary/Lab4/` | `Lab4/DOC/readme.txt:2-3` is signed "Adar Shapira 209580208 / Yehonatan Dadkha 211468582". `Lab3/DOC/pre3.pdf` is a Hebrew pre-lab report by the same authors. |
| **The students, Lab 5** | `Auxiliary/Lab 5/{DUT,TB,SIM,Quartus}/`, `Auxiliary/Lab 5 - as submitted/` | Derived from the lecturer baseline; `DOC/readme.txt` in the submitted tree is signed by the same authors. |
| **Third party** | `Auxiliary/USART Material/UART_FPGA_option{1,2}/`, every `PLL.vhd` | `UART_FPGA_option1/LICENSE` is MIT, `Copyright (c) 2015 Jakub Cabal`. Option 2 carries no licence at all. `PLL.vhd` line 15: `-- THIS IS A WIZARD-GENERATED FILE. DO NOT EDIT THIS FILE!` |

**`Auxilary/` (one `i`) is a mixed folder.** It is Hanan's Lab 5 distribution *plus* two student
archives dropped inside it. A tree-wide grep for the student identifiers over all of `Auxilary/`
returns exactly two hits, both in `Lab4/DOC/readme.txt:2-3`; the copyright grep returns ten, all in
`Auxilary/DUT/` and `Auxilary/TB/`. `QUARTUS/pinPlanner/DE2-115_pinLocation.txt` is also student
work — it references the `RSTPOL` generate, which does not exist in the lecturer baseline.

### What Hanan actually supplied as code

Twelve source files. The baseline is a **single-cycle RV32I core only**: no multiplier, no pipeline,
no debug instrumentation. Verified by full recursive listing plus a grep for
`STCNT|FHCNT|BPADDR|BPTRIGGER` over `Auxilary/{DUT,TB,SIM,QUARTUS}`, which matched only the
student-written pin note.

| File | Role |
| --- | --- |
| `Auxilary/DUT/RV32I_CORE.vhd` | Structural single-cycle top: `IFE`/`ID`/`CTL`/`EXE`/`MEM`, a conditional PLL, and the `mclk_cnt_q` counter |
| `Auxilary/DUT/CONTROL.VHD` | Combinational RV32I decoder; per-instruction detectors ANDed against `INST_*_MASK`, driving a 5-bit `ALUOp` priority chain |
| `Auxilary/DUT/IDECODE.VHD` | 32×32 register file, field extraction, I/S/SB/U/UJ immediate generation, write-back mux |
| `Auxilary/DUT/EXECUTE.VHD` | ALU, a 4-stage unrolled barrel shifter, the six branch comparators, branch-address adder |
| `Auxilary/DUT/IFETCH.VHD` | PC, PC+4, next-PC mux tree, reset-hold flop, `altsyncram` ITCM |
| `Auxilary/DUT/DMEMORY.VHD` | DTCM wrapper — one `altsyncram` clocked on `not(clk)` |
| `Auxilary/DUT/const_package.vhd` | Immediate pads, opcode-type constants, 18 `ALU_*` opcodes, 60+ `INST_*`/`INST_*_MASK` pairs already covering the **entire M extension**, CSR, fence and privileged instructions |
| `Auxilary/DUT/cond_compilation_package.vhd` | The eight `G_*` knobs every entity's generics default to |
| `Auxilary/DUT/aux_package.vhd` | Component declarations |
| `Auxilary/DUT/PLL.vhd` | ALTPLL MegaWizard output (third-party generated) |
| `Auxilary/TB/tb_RV32I.vhd` | Testbench — no assertions, no `report`, no memory dump, no stop condition |
| `Auxilary/SIM/RV32I.do` | Wave configuration only; no compile or run automation |

Consequence: the entity names the submission mandates (`RV32IM_CORE`, `RV32IM_PIPE_CORE`) do not
exist in the baseline — its top is `RV32I_CORE`. Every one of them is student-created.

---

## 2. The component inventory

Verdicts: **use as is** = drop in unchanged; **adapt** = start from it, changes required;
**reference only** = read for understanding, write our own; **write new** = nothing exists.

### 2.1 CPU core and ISA

| Component | Requirement | Supplied | Path | Provenance | Verdict |
| --- | --- | --- | --- | --- | --- |
| Single-cycle RV32IM core | §3 | yes | `Auxiliary/Lab 5/DUT/RV32IM_sc/` (11 files) | students, Lab 5 | **adapt** — five decode defects, no `div`/`rem`/`mulh*` |
| RV32I baseline | — | yes | `Auxilary/DUT/` (10 files) | **Hanan** | **reference** — authority for one of the five defects only |
| 5-stage pipeline core | bonus 10% | yes | `Auxiliary/Lab 5/DUT/RV32IM_pipeline/` (13 files) | students, Lab 5 | **adapt** — a rewrite, not a derivative |
| Hazard / forward units | bonus 10% | yes | `RV32IM_pipeline/HAZARD_UNIT.vhd`, `FORWARD_UNIT.vhd` | students, Lab 5 | **adapt** — must gain divider and memory stalls |
| 16×16 multiplier | §6.iii, LAB5 Fig 6 | yes | `RV32IM_sc/MUL16.vhd` (86 lines) | students, Lab 5 | **adapt** — see 2.6 |
| Instruction masks for the whole M extension | §2 | yes | `const_package.vhd:243-273` | **Hanan** | **use as is** — `INST_MUL`, `MULH`, `MULHSU`, `MULHU`, `DIV`, `DIVU`, `REM`, `REMU` and their masks all already present |
| Barrel shifter | §2 | yes | `Auxilary/Lab4/DUT/Shifter.vhd` | students, Lab 4 | **reference** — generic n-bit structural MUX matrix; candidate `sra` repair |
| Register file | §3 | yes | `IDECODE.vhd` (32×32) | Hanan | **use as is** |
| — | — | yes | `Auxilary/Lab3/DUT/RF.vhd` (16×16) | students, Lab 3 | **reference only** — wrong width |
| `div`/`divu`/`rem`/`remu`, `mulh*` | §2 | **no** | — | — | **write new** |
| Byte enables, sub-word load/store | §2 | **no** | — | — | **write new** — see 2.6 |

### 2.2 The five decode defects — provenance re-derived

Each file diffed against the baseline with comments and whitespace stripped.

| # | Defect | Site in `RV32IM_sc` | Origin |
| --- | --- | --- | --- |
| 1 | `andi` writes 0; `ori` computes AND | `CONTROL.vhd:142` | **Student regression.** Baseline `Auxilary/DUT/CONTROL.VHD:141` is correct: `ALU_AND WHEN and_w or andi_w ELSE`. `ori_w` then appears twice in one conditional chain, so the earlier `ALU_AND` arm wins. |
| 2 | `lui` writes 0 | `const_package.vhd:27` | **Lecturer's baseline.** `Auxilary/DUT/const_package.vhd:27` is identical: `UTYPE_OPC := "0010111" and "0110111"`, which evaluates to `"0010111"` — `auipc` only. `IDECODE` uses it as an exact `select` choice, so `lui`'s `"0110111"` matches nothing. |
| 3 | Every load addresses `rs1 + 0` | `IDECODE.vhd:97-104` | **Lecturer's baseline.** `Auxilary/DUT/IDECODE.VHD:94-101` is the identical `with opc_w select` with six arms and no LOAD (`0000011`) arm, so the immediate falls to `(others => '0')`. |
| 4 | `sra` ≡ `srl` | `EXECUTE.vhd:200` | **Lecturer's baseline.** `Auxilary/DUT/EXECUTE.VHD:179` is identical: `brl_shr_pad_r <= 32x"FFFF"`, which in VHDL-2008 is `0x0000FFFF`, so bit 31 is `'0'`. Only `brl_shr_pad_r(31 DOWNTO 16)` is ever read (line 214), so the sign never propagates. |
| 5 | `sltu`/`sltiu`/`bltu`/`bgeu` compare signed | `EXECUTE.vhd:10` | **Lecturer's baseline.** `Auxilary/DUT/EXECUTE.VHD:9` is identical: `USE IEEE.STD_LOGIC_SIGNED.ALL`, which makes line 78's `ltu_res_w <= '1' WHEN ain_w < bin_w` a signed comparison. |

**Four of the five are in code Hanan distributed** and were inherited unchanged by both student
cores. The baseline is a repair source for exactly one. The other four we design ourselves, and the
report presents them as supplied defects rather than as failures of our RTL. Raised as open
question 13.

Why they stayed hidden: Lab 5's own test1–test4 are add / mul / xor programs and contain zero
`lui`, zero `andi`, zero `sra` and zero loads with a non-zero offset. Every one of the five is
exercised by the final-project benchmarks.

### 2.3 Peripherals

| Component | Requirement | Supplied | Path | Provenance | Verdict |
| --- | --- | --- | --- | --- | --- |
| Seven-segment decoder, active-low | §5, Fig 5 | **yes** | `Auxilary/Lab4/DUT/hex_decoder.vhd` (36 lines) | students, Lab 4 | **use as is** — full 0–F table, all-off default, comment at line 13 states DE2-115 active-low |
| PWM output unit | §6.ii, Fig 8 | **yes** | `Auxilary/Lab4/DUT/pwm.vhd` (60 lines) | students, Lab 4 | **adapt** — 16→32 bit; re-map period `Y`→`BTCL0`, duty `X`→`BTCL1`; drop the unused Toggle mode or map it to `BTINT` |
| Bidirectional data bus | Fig 1, which links to it as "Bi-directional Data BUS (reminder)" | **yes** | `Auxilary/Lab3/DUT/BidirPin.vhd` (20 lines) | students, Lab 3 | **use as is** with `width => 32` |
| ITCM unregistered / DTCM registered pattern | Fig 3 | yes | `Auxilary/Lab3/DUT/progMem.vhd`, `dataMem.vhd` | students, Lab 3 | **reference** — explains why Lab 5's DTCM runs on `not(clk)` |
| Basic Timer core (`BTCNT`, `BTSSEL` mux, `BTCL0/1` latches, comparators, capture) | §6.ii, Fig 7 | **no** | — | — | **write new** |
| Interrupt controller (`IE`/`IFG`/`TYPE`, priority, `INTR`/`INTA`) | §6.v | **no** | — | — | **write new** |
| CPU interrupt entry FSM | §6.v protocol | **no** | — | — | **write new** |
| MMIO address decoder | §5, §6, Fig 5 | **no** | — | — | **write new** — Figure 5 gives the schematic |
| GPIO buffer registers (LEDR, HEX×6, SW, PB) | §5, §6 | **no** | — | — | **write new** |
| Division accelerator | §6.iii, Fig 9 | **no** | — | — | **write new** — Figure 9 specifies it completely |
| Subtractor for the divider | §6.iii | yes | `Auxilary/Lab4/DUT/AdderSub.vhd`, `FA.vhd` | students, Lab 4 | **reference** — generic n-bit ripple-carry add/sub |
| Edge detector / one-shot for KEY1–3 | §6.i | **no** | — | — | **write new**. Board keys are already debounced (Fig 6 shows a Schmitt trigger), so what is missing is edge detection, not debounce. Zero hits for `edge_detect`/`one_shot` anywhere. |
| Two-flop CDC synchronizer | §6.iii, Fig 10a/10b | **no** | — | — | **write new**. The only real double-register in the material is inline inside third-party `UART_FPGA_option2/VHDL/DUT/UART_RX.vhd:46-56`. **`IFETCH.vhd:73-82` is labelled "rst_i synchronization" but is a single flop with an async preset — not a synchronizer.** |

### 2.4 Clocking

| Component | Requirement | Supplied | Path | Provenance | Verdict |
| --- | --- | --- | --- | --- | --- |
| ALTPLL wrapper | Fig 1 | yes, three copies | `RV32IM_sc/PLL.vhd`, `Auxilary/DUT/PLL.vhd` (md5-identical, `a12064f2…` — the only file nobody has touched), `Auxilary/Lab4/DUT/pll.vhd` | third-party generated | **adapt** |
| Multi-output clock tree (`mclk`, `accelclk`, `smclk`) | Fig 1 | **no** | — | — | **write new.** All three ALTPLL copies expose only `c0`. The megafunction must be regenerated with `c1`/`c2`. |
| Worked non-trivial PLL ratio | — | yes | `Auxilary/Lab4/DUT/pll.vhd` — 50 MHz → 2 MHz | students, Lab 4 | **reference** |

### 2.5 UART (bonus 20%)

§6.iv says "You are given a VHDL design code that needs to be adapted", so this is supplied
material we are expected to use.

| Aspect | Option 1 | Option 2 |
| --- | --- | --- |
| Source | jakubcabal/uart-for-fpga v1.1 (20 Dec 2018) | nandland, VHDL + Verilog copies |
| Licence | **MIT**, `LICENSE:1-3` | **none anywhere** — only a URL comment |
| Baud generation | `DIVIDER_VALUE := CLK_FREQ/(16*BAUD_RATE)`, `rtl/uart.vhd:58` — **compile-time** | compile-time |
| Oversampling | 16×, samples mid-bit (`rx_ticks = "0111"`, `rtl/comp/uart_rx.vhd:78`) | — |
| Frame | 1 start / 8 data / 1 stop, LSB-first (`rx_data <= UART_RXD & rx_data(7 downto 1)`, `uart_rx.vhd:113`) | same |
| Parity | yes — none/even/odd/mark/space, `rtl/comp/uart_parity.vhd` | none |
| Error flags exported | one of three: `FRAME_ERROR <= NOT UART_RXD` (`uart_rx.vhd:162`). Parity error computed at `:139` but **never exported** | none |
| Testbench | `tb & sim/uart_tb.vhd` — no `assert`, no `report`. `example/uart_loopback_tb.vhd` is **broken as shipped**: it port-maps `DIN_RDY` and `FRAME_ERR`, which the `UART_LOOPBACK` entity does not declare | none |

**Verdict: adapt Option 1.** It is the only one with a usable licence, the only one with parity, and
its baud divider is isolated in one 20-line process, which is what makes runtime 9600/115200
switching tractable.

**Write new** — absent from both options: separate `RXBUF`/`TXBUF` buffer registers, overrun (`OE`)
logic, a parity-error output port, an aggregate `BUSY` status, an `SWRST` path, a runtime baud
register, and any MMIO or interrupt wrapper. The bonus is a shift-register core we adopt plus a
register layer we write.

### 2.6 Multiplier scope

`MUL16.vhd` is 16×16 → 32 **unsigned**, built from four 8×8 partial products — exactly LAB5
Figure 6's `P0..P3`, `M = P1+P2`, `RESULT = P0 + (M<<8) + (P3<<16)`. And `EXECUTE.vhd:93-94` feeds
it only the **lower half-words**:

```vhdl
a_i => ain_w(DATA_BUS_WIDTH/2-1 DOWNTO 0),
b_i => bin_w(DATA_BUS_WIDTH/2-1 DOWNTO 0)
```

So `mul` is correct only when both operands fit in 16 bits unsigned, and `mulh`/`mulhsu`/`mulhu`
are not decoded at all despite their masks existing. Whether a conformant 32×32 is required is
open — LAB5 calls the scope "MULDIV partial" — but a full 32×32 multiplier does not exist anywhere
in the material.

### 2.7 Verification and tooling

| Component | Supplied | Path | Provenance | Verdict |
| --- | --- | --- | --- | --- |
| MCU testbenches | yes | `Auxiliary/Lab 5 - as submitted/TB/RV32IM_{sc,pipeline}/` | students, Lab 5 | **adapt** — waveform-oriented, not self-checking |
| Compile order | yes | `Auxiliary/Lab 5 - as submitted/SIM/*/compile.do` | students, Lab 5 | **adapt** — the explicit VHDL compile order is valuable |
| Run / dump / batch scripts | yes | same dir: `run_test.do`, `mem_dump.do`, `batch_verify.do`, `wave.do`, `modelsim.ini` | students, Lab 5 | **adapt**. `batch_verify.do` only echoes results — no non-zero exit on mismatch. |
| Measured baseline dumps | yes | `SIM/*/DTCM_test1..4.mem` | students, Lab 5 | **use as is** — diff a reproduced run against these |
| Assertion-based TB pattern | yes, barely | `Auxilary/Lab3/TB/tb_top.vhd:86` (timeout, `severity failure`) and `:110` (`assert false report "Simulation Completed Successfully"`) | students, Lab 3 | **reference** — **the only assertion usage in the entire material**; the seed for the self-checking style the rules require |
| Self-checking / scoreboard infrastructure | **no** | — | — | **write new** |
| SDC | yes | `Auxilary/QUARTUS/SDC/RISCV_simple.sdc` — `create_clock -period 20 [get_ports {clk_i}]` plus `derive_pll_clocks -create_base_clocks` | **Hanan** | **adapt** |
| — | yes | `Auxiliary/Lab 5 - as submitted/Quartus/*/*.sdc`, and `Auxilary/Lab4/Quartus/Lab4_{hw,perf}.sdc` | students | **adapt** |
| Two-revision PPA structure | yes | `Auxilary/Lab4/DUT/perf_wrapper.vhd` + `Lab4_perf.sdc` (pinless, SignalTap off) + `Lab4_hw.sdc` (pinned, SignalTap on) | students, Lab 4 | **adapt** — exactly the structure the three mandatory PPA rows need |
| SignalTap file | yes | `Auxilary/Lab4/Quartus/stp1.stp`, plus the Lab 5 `.stp` | students | **adapt** |
| Board-level top wrapper | yes | `Auxilary/Lab4/DUT/fpga_hw_interface.vhd` | students, Lab 4 | **reference** — pattern for the MCU top |
| Report structure | yes | `Auxiliary/Lab 5 - as submitted/DOC/Report_lab5.pdf` | students, Lab 5 | **reference** — LAB5 §9 and the final project §10 require the same items |
| Architecture figures | yes | `Auxiliary/Lab 5 - as submitted/report_figs/` (8 PNGs) | students, Lab 5 | **adapt** |
| PPA / waveform evidence | yes | `Auxiliary/Lab 5 - as submitted/Screenshots/{Quartus,ModelSim}/{SC,Pipeline}/` | students, Lab 5 | **reference** — templates for the mandatory screenshots |
| ISMCE procedure | yes | `Auxilary/Validation using ISMCE.pdf` | **Hanan** | **use as is** |
| Toolchain procedure | yes | `Auxilary/RISC-V Tool-Chain.pdf` | **Hanan** | **use as is** |
| Working ModelSim procedure | yes | `Auxiliary/Lab 5 - as submitted/ModelSim_Testing_Guide.md` | students, Lab 5 | **use as is** |

### 2.8 Earlier labs — what exists and what does not

The question "was earlier-lab code supplied that we should be reusing" resolves as follows.

- **Lab 3** — present in full: `DOC`, `DUT`, `SIM`, `TB`, plus `SW-QA/Ex5` and `Ex6` with `.asm`/`.c`
  sources and matching ITCM/DTCM images. A multi-cycle 1-BUS CPU with a 16×16 register file.
- **Lab 4** — present in full: `DOC`, `DUT`, `SIM`, `TB`, `Quartus`. The richest source of reusable
  leaf modules in the whole tree.
- **Lab 1** — survives only as the 8-bit combinational ALU absorbed into Lab 4
  (`top.vhd`/`AdderSub`/`Logic`/`Shifter`/`FA`), identified by `Lab4/DOC/readme.txt:12`.
- **Lab 2** — **completely absent.** No folder, no file, no header, no README mention.

---

## 3. Hazards recorded while surveying

1. **`Auxiliary/Lab 5/` is not the submitted Lab 5.** Two additions exist only in the project copy:
   the `RSTPOL` generate in both core tops, and the DE2-115 pin assignments in both `.qsf` files.
   Reproduce the baseline against `Auxiliary/Lab 5 - as submitted/`. With `RSTPOL` present and
   `G_MODELSIM = 0`, the testbench's active-high reset is inverted and the core never leaves reset.
2. **`G_MODELSIM` is a manual toggle**, documented in
   `Auxiliary/Lab 5 - as submitted/DOC/readme.txt`: set it to `0` for Quartus and back to `1` for
   ModelSim. The baseline ships at `0`.
3. **Absolute Windows paths are baked into the RTL** at four sites —
   `init_file => "C:\TestPrograms\Quartus21_1\app_bin\{ITCM,DTCM}.hex"` in `IFETCH.vhd:64` and
   `DMEMORY.vhd:50` — and the `.do` scripts stage benchmarks by copying from
   `C:/TestPrograms/Quartus21_1/test$N/bin/`. This is the **documented working convention**, not an
   accident; `testN/RARS/DTCM.h` holds the goldens. Do not change it while reproducing the
   baseline. Parameterise only before staging the submission ZIP, which forbids absolute-path
   dependencies.
4. **`mem_dump.do` reaches into simulator internals** (`.../data_memory/MEMORY/m_mem_data_a`),
   which version-locks it to ModelSim ASE 2020.1.
5. **The single-cycle critical path has only half a clock period.** The Lab 5 `critical_path.png`
   shows ITCM `porta_address_reg` → `ID|Mux19~2/3/6/9` → DTCM `porta_address_reg`, relationship
   **20.000 ns** with an **inverted** latch clock, slack 1.351 ns. The MMIO decoder of §2.3 lands
   directly in that path.
6. **Two similarly-spelled benchmark folders.** `Auxiliary/Benchmark Apps/Intrrupt-based IO/` is
   the current revision; `_superseded/Interupt-based IO/` is the older one. One letter apart — a
   real hazard for a `.do` script path.
7. **Use `bin/M9K-intel/*.hex` for ITCM, never `bin/Hexadecimal-Text/ITCM.h`.** The two formats are
   different programs: the `.hex` images use `auipc imm = 0x00000` (text base 0), the `.h` images
   retain RARS's `0x3000` base. The `.h` files are the DTCM golden text only — which is exactly
   what §8.c.i asks for.
8. **The project's own working folders are empty.** `DUT/`, `TB/`, `SIM/`, `Quartus/`,
   `Screenshots/` and `209580208_211468582/` each contain only a `.gitkeep`.

---

## 4. Summary

| Verdict | Count | Components |
| --- | --- | --- |
| **use as is** | 6 | `hex_decoder.vhd`; `BidirPin.vhd` (`width => 32`); the M-extension instruction masks; the 32×32 register file; the measured baseline dumps; the two lecturer procedure PDFs |
| **adapt** | 14 | both CPU cores, hazard/forward units, `MUL16`, `pwm.vhd`, the ALTPLL, UART option 1, both testbenches, the `.do` scripts, the SDCs, the two-revision PPA structure, the `.stp`, the report structure, the architecture figures |
| **reference only** | 8 | the RV32I baseline, `Shifter.vhd`, `AdderSub.vhd`/`FA.vhd`, `Lab3/RF.vhd`, `progMem`/`dataMem`, `fpga_hw_interface.vhd`, `Lab4/pll.vhd`, `Lab3/tb_top.vhd` assertions |
| **write new** | 12 | divider accelerator, Basic Timer core, interrupt controller, CPU interrupt FSM, MMIO decoder, GPIO buffer registers, `div`/`rem`/`mulh*`, byte enables and sub-word load/store, edge detectors, CDC synchronizer, multi-output clock tree, the UART register layer |

The roadmap assumed the seven-segment decoder, the PWM unit and the bidirectional bus would all be
written from scratch. They exist. Conversely it under-scoped sub-word load/store and the UART
register layer, both of which are larger than a single step.
